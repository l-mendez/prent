// Disabled — see agent/tools/bash.ts. The original agent kept its short plan internal
// (per <before_using_tools>), with no durable todo tool.
import { disableTool } from "eve/tools";

export default disableTool();
